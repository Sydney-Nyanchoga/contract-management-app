specVersion: "v1"
serviceId: "contract-management-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "contract-management-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
